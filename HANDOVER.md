# 인수인계 — 코드코리아 기업 홈페이지 (kodekorea-webpage)

작성 2026-09-08 · 마지막 커밋 `232d70c` · 마지막 배포 2026-09-04

> 이 문서는 **코드만 봐서는 알 수 없는 것**을 적었습니다.
> 코드 구조는 각 파일 상단 주석에 이유까지 적어뒀으니 그쪽을 보시면 됩니다.
> 확실하지 않은 것은 **(추정)** 으로 표시했습니다.

---

## 1. 한 장 요약

| | |
|---|---|
| 무엇 | 코드코리아 **자체** 기업 홈페이지 (수주 프로젝트가 아니라 우리 회사 소개 사이트) |
| 상태 | **운영 중.** 정식 도메인에 라이브. 단 **검색엔진 색인은 일부러 막아둔 상태** |
| 정식 주소 | `https://www.kodekorea.kr` (apex `kodekorea.kr`은 307로 www로 넘김) |
| 기술 | Next.js 15 App Router · 정적 export(`output: 'export'`) · TypeScript · Tailwind 4 · GSAP + Lenis |
| 저장소 | `https://github.com/mslopgk/kodekorea-webpage.git` — **원격 있음, `master` 푸시 완료** |
| 결정권자 | 대표 조성호 (010-2751-7966 / seongho.cho@kodekorea.kr) |
| 지금 급한 것 | ① 개인정보처리방침 없음 → 색인 못 엶 ② Netlify 배포 차단(크레딧) ③ 캡처 이미지가 기관명 마스킹을 무력화 |

### 동작하는 것

- 홈(`/`) 전체 — 히어로, 창업팀 프로토타입 섹션, 사업축 3개, 실적 대시보드 20건, 역량표, 문의, 법정 사업자정보
- 프로젝트 상세 페이지 **20개** (`/project/<slug>/`) — 정적 페이지라 견적서에 링크 붙여 보낼 수 있음
- 상시 모션 전부 (텍스트 순회, 파형, 티커, 스크롤 연출) + `prefers-reduced-motion` 대응
- 3곳 배포 파이프라인 (Vercel / Netlify / 자체 서버)

### 동작하지 않거나 없는 것

- **문의 폼 없음.** `mailto:` 링크만 있습니다. 폼을 만들면 개인정보처리방침이 법적 필수가 됩니다
- **개인정보처리방침 페이지 없음.** 푸터 링크가 `#`로 비어 있습니다
- **아이콘 세트 없음.** 브랜드 방향(컬러·로고) 미확정이라 손대지 않았습니다
- **실기기 모바일 검증 안 됨.** 미디어쿼리는 작성했지만 실제 폰에서 본 적 없습니다
- **Netlify 신규 배포 차단** — 계정 크레딧 초과 (403)
- 실적 대시보드의 "실적 추가"는 **브라우저 localStorage에만** 저장됩니다. 서버 저장 아님

---

## 2. 이 프로젝트의 제일 중요한 규칙

**근거 없는 사실을 사이트에 쓰지 않습니다.** 이건 취향이 아니라 이 프로젝트에서 크게 깨진 뒤 세운 원칙입니다.

`content/site.ts` 상단에 이 규칙이 적혀 있고, 모든 수치에는 `source` 필드가 붙어 있습니다.
수치를 추가하실 때 근거를 못 대면 **그 줄을 만들지 마세요.**

무슨 일이 있었는지:

| 사건 | 내용 |
|---|---|
| 초안 수치가 우리 것이 아니었다 | 첫 시안에 쓴 `13,022`, `BubbleMap`, `TeamHub`, `$39` 등은 **다른 기관 클라이언트의 샘플 데이터**였습니다. 대표 회신본으로 확인되어 전부 제거했고, 빌드 산출물에서 0건인지 검증했습니다 |
| 가짜 계측값을 그렸다 | `UPTIME 9982h`, `NODES 12`, `RTT 38ms`, 스파크라인 추이 — 전부 지어낸 값이었습니다. 대표 지적("의미 없는 요소")으로 **전량 삭제**했습니다. 지금 남은 모션은 실제 목록을 순회하거나(`cycle.tsx`) 어떤 수치도 주장하지 않는 추상 파형(`ambient.tsx`)뿐입니다 |
| 두봇은 되살렸다 | 근거 없다고 판단해 지웠는데, 메일 확인 결과 **국립부산과학관 계약 과업의 실습 장비(Dobot)** 였습니다. 지금은 계약 확인 실적으로 들어가 있습니다 |

### 클라이언트 실명은 마스킹 상태입니다

`content/site.ts`의 `ANONYMIZE = true`. 실명 노출 **서면 동의를 받은 기관이 한 곳도 없습니다.**
기관명은 전부 `client(실명, 마스킹명)` 을 통과하므로, 이 플래그 한 줄만 바꾸면 실명으로 전환됩니다.
**임의로 끄지 마세요.** 공공기관 실적은 관행상 공개 가능성이 높지만 판단은 대표가 합니다.

---

## 3. 파일·폴더 지도

| 위치 | 무엇이 있나 | 왜 거기 있나 |
|---|---|---|
| **`content/site.ts`** | **사이트의 모든 문구·수치·실적. 약 1,000줄** | 단일 콘텐츠 소스. 시안 4벌이 전부 이 파일만 읽습니다. 컴포넌트에 문구 하드코딩 금지 |
| `content/projects.ts` | 상세 페이지용 프로젝트 인덱스 | `pillars`(캡처·수치 보유)와 `portfolio`(근거 등급 보유)를 한 모양으로 합칩니다. slug 중복은 빌드 때 throw |
| **`app/page.tsx`** | **실물 홈페이지 루트** | `app/c/v2/V2Page.tsx`를 렌더합니다. 한때 루트가 시안 선택 페이지여서 지적받고 고쳤습니다 |
| **`app/c/v2/`** | **현재 쓰는 디자인의 실제 구현** | `V2Page.tsx`(마크업) · `cycle.tsx`(실제 목록 순회 모션) · `ambient.tsx`(추상 파형) · `Choreo.tsx`(GSAP·Lenis) · `Dashboard.tsx`+`posts.ts`(실적 표) · `Plate.tsx`(절차적 도면) · `ticker.ts`(공유 rAF) |
| `app/project/[slug]/` | 프로젝트 상세 페이지 (20개 정적 생성) | `generateStaticParams`로 slug마다 실제 HTML을 만듭니다 |
| `app/c/hle` `hut8` `robin` `sentra` | **버려진 시안 4벌** | 대표에게 링크를 이미 공유했어서 살려뒀습니다. `noindex`. **`nav[0]`~`nav[3]`을 위치 인덱스로 참조**하니 §7 함정 참고 |
| `app/concepts/` | 시안 비교 인덱스 | 루트에서 밀려난 페이지. `noindex` |
| `lib/motion.ts` `lib/text.ts` | 공용 훅 (`useReducedMotion`, `useCountUp`, `useScramble`) | 시안 4벌이 공유 |
| `public/work/` | 실적 캡처 2장 | `arise-hero.jpg`, `arise-layout.jpg`. **§7의 마스킹 문제 있음** |
| **`docs/portfolio-from-mail.md`** | **실적의 근거 원장. 158줄** | 회사 메일 6,305통에서 실적을 뽑은 방법과 근거 등급. 실적을 건드릴 땐 반드시 이 문서부터 |
| **`docs/hosting.md`** | **자체 서버 운영 매뉴얼. 131줄** | SSH 접속법, 릴리스 구조, Caddy 지시자, 색인 차단 해제 체크리스트 |
| `docs/client-materials-01.md` | 대표 자료 회신본 사본. 481줄 | 무엇이 공개 가능한지의 원천. `site.ts`가 이걸 근거로 삼습니다 |
| `docs/design-review-01.md` | 디자이너 1차 평가 | 카드 갤러리 금지, 안전한 타입 스케일 금지 등 지적사항 |
| `docs/references.md` | 레퍼런스 사이트 분석 4건. 508줄 | hut8 / Sentra / hle.io / robin-thomas.me |
| `netlify.toml` · `vercel.json` · `docs/hosting.md`의 Caddy | **배포처 3곳의 헤더 설정** | 세 곳 값을 일치시켜 뒀습니다. 한 곳만 고치면 어긋납니다 |
| `vercel.json.README.md` | vercel.json 설명 | Vercel 스키마가 `comment` 키를 거부해서 설명을 밖으로 뺐습니다 |
| `.env.local` | `VERCEL_OIDC_TOKEN` 하나 (Vercel CLI 자동 생성) | 이 프로젝트가 쓰는 유일한 리포 내 토큰. §9 참고 |
| `.vercel/project.json` | Vercel 프로젝트 연결 정보 | 이게 있어야 `vercel deploy`가 맞는 프로젝트로 갑니다 |
| `_보관/` | 재생성 가능한 산출물·캐시 | 지운 게 아니라 옮겼습니다. `_보관/README.md` 참고 |

---

## 4. 실행 · 빌드 · 배포

### 처음 받았을 때

```bash
npm install          # node_modules는 인계 패키지에서 제외됨
npm run dev          # http://localhost:3000
```

Node 22 기준으로 작업했습니다 (`netlify.toml`에 `NODE_VERSION = "22"`).

### 빌드

```bash
npm run build        # 정적 export -> out/ (95개 파일)
npx tsc --noEmit     # 타입 검사
```

`output: 'export'`라서 서버가 필요 없습니다. `out/`을 아무 정적 호스팅에 올리면 됩니다.

### 빌드 결과 눈으로 확인하기

**dev 서버로 확인하지 마세요.** dev 서버가 낡은 CSS를 내주는 일이 반복돼서 없는 버그를 몇 시간 쫓았습니다.

```bash
cd out && python -m http.server 4321   # 그 다음 http://127.0.0.1:4321
```

끝나면 **서버를 반드시 끄세요.** 켜둔 채로 다시 빌드하면 `out/` 삭제가
`Device or resource busy`로 실패합니다.

### 배포 — 3곳

| 배포처 | 명령 | 상태 (2026-09-08 확인) |
|---|---|---|
| **Vercel (정식)** | `npx vercel deploy --prod --yes` | **정상.** DNS가 여기를 가리킴 = 이게 실서비스 |
| Netlify (검토용) | `npx netlify-cli deploy --prod --dir=out --no-build` | **차단.** 403 크레딧 초과 |
| 자체 서버 (예비) | `npm run deploy` | 정상. 단 **DNS 미연결**이라 접속자는 없음 |

Vercel/Netlify는 CLI 로그인이 필요합니다 (`vercel login`, `netlify login`).
작업 당시 계정: Vercel `kgpolsmai-8795`, Netlify `kgpolsm.ai@gmail.com`. **(추정)** 대표 계정일 것.

#### Netlify는 반드시 `--no-build --dir=out`으로

`@netlify/plugin-nextjs`가 자동으로 붙는데, 정적 export에는 할 일이 없고
`/_next/image` 리다이렉트만 심습니다. 미리 빌드한 `out/`을 그대로 올리는 게 맞습니다.

#### 자체 서버

```bash
npm run deploy            # 빌드 -> tar로 stdin 전송 -> 심볼릭 링크 전환
npm run deploy:list       # 현재 릴리스와 목록
npm run deploy:rollback   # 직전 릴리스로 되돌리기
```

- 서버 `156.228.4.156` (호스트명 `general-server-1`), 사용자 `ubuntu`, 키 `~/.ssh/id_ed25519_new`
- **`ubuntu@blend.kodekorea.kr`로는 SSH가 안 됩니다.** Cloudflare 프록시 뒤라 80/443만 통과합니다. **오리진 IP로 붙으세요**
- 배포는 파일 덮어쓰기가 아니라 **심볼릭 링크 전환**입니다. 배포 중 반쯤 갱신된 화면이 노출되지 않고, 롤백은 링크 되돌리기로 끝납니다
- `index.html`이 없는 아카이브는 서버 스크립트가 거부합니다. 릴리스는 최근 5개만 보관
- 같은 서버에 다른 서비스 **27개**가 돌고 있습니다. Caddy 설정은 맨 아래 두 블록만 우리 것입니다. `sudo caddy validate` 후 `systemctl reload`(restart 아님)

### 참고 — 메일 서버는 별개 장비입니다

실적을 뽑을 때 쓴 `mail.kodekorea.kr`은 `156.228.4.147`의 **다른 서버**이고 mailcow로 돌아갑니다.
웹사이트 배포와는 무관합니다. 방법은 `docs/portfolio-from-mail.md`에 있습니다.

---

## 5. 상대 기관 · 담당자 · 약속

### 이 프로젝트의 상대

수주 프로젝트가 아니라 **자사 홈페이지**입니다. 유일한 결정권자는 **대표 조성호**입니다.

| 항목 | 값 |
|---|---|
| 대표 | 조성호 |
| 연락 | 010-2751-7966 · seongho.cho@kodekorea.kr |
| 사업자 | 개인사업자 · 679-09-00696 (체크섬 검증 완료) |
| 주소 | 부산광역시 금정구 중앙대로1793번길 38, 204호 |

### 대표가 준 지시 중 처리된 것

| 시점 | 지시 | 상태 |
|---|---|---|
| 2026-08-06 | "창업팀 프로토타입 개발" / "프로토타입 제작 스타트업 전문" 노출. **급하게 보내야 함** | **완료** — 히어로 아래 `#prototype` 섹션. meta description도 이걸 앞세워 링크 미리보기에 뜨게 함 |
| 2026-08-03 | 사업자등록번호 반영 | 완료 (법정 사업자정보 블록) |
| 2026-08-03 | 메일 계정 전수 검토해 포트폴리오화 | 완료 (실적 20건, 상세 20페이지) |

### 대표 결정을 기다리는 것

회신본 부록 B 항목 + 작업 중 제가 올린 질문들입니다. **답을 못 받은 상태로 넘깁니다.**

- 로고 리뉴얼 여부 / 브랜드 가이드 유무 / **브랜드 컬러 확정** → 현재 액센트 오렌지는 제가 임의로 넣은 값입니다
- 문의 폼 수신처 (폼 만들면 개인정보처리방침 선행 필수)
- 개인정보처리방침 초안 승인
- 클라이언트 실명 노출 동의 (기관별)
- **정식 URL 방향** — §7 참고
- **캡처 이미지 마스킹** — §7 참고

### 실적에 등장하는 기관

메일 근거로 확인된 계약·청구·수행처입니다. 전부 마스킹 상태이고,
실명은 `content/site.ts`의 `client()` 안에만 있습니다. 목록과 근거 등급은 `docs/portfolio-from-mail.md`.

**세금계산서 발행 거래처 20곳**(공공·교육 14 / 민간 6, 2025-02~2026-06)이
가장 다투기 어려운 숫자여서 실적 헤더에 크게 뒀습니다.

---

## 6. 왜 이 사이트가 이렇게 생겼나

디자이너 1차 평가 지적을 반영한 결과입니다. 되돌리지 마세요:

- **카드 갤러리 금지** — "프로젝트가 전부 카드 갤러리라 브랜드 개성이 없다". 그래서 실적을 **계측기 로그 형태의 밀도 높은 표**로 만들었습니다
- **안전한 타입 스케일 금지** — 히어로 3동사를 크게 갑니다
- **긴 문장 금지** · **스크롤 리듬 필요**
- 명암 반전은 **딱 두 번**. 각 반전에 이유가 있습니다 (다크→라이트: 읽는 구간 / 라이트→다크: 증거 구간)
- 방향은 "시안 B를 베이스로, 시안 A의 정보 구조를 일부 차용" — 그 결과가 지금의 `/c/v2`

한글 타이포: Pretendard(한글) + JetBrains Mono(라틴·숫자), 자간은 **em 기반으로 분리**
(한글 ≤0.02em, 라틴 0.03~0.04em). 한글을 라틴만큼 좁히면 뭉칩니다.

---

## 7. 함정과 주의사항

### ⚠️ 색인 차단을 함부로 풀지 마세요

`X-Robots-Tag: noindex, nofollow` 가 **3곳 전부**에 걸려 있습니다
(`vercel.json`, `netlify.toml`, 서버 Caddyfile). 아래가 해결되기 전에 풀면 안 됩니다:

1. **개인정보처리방침** — 없습니다. 문의 폼으로 개인정보를 받는 순간 법적 필수
2. **클라이언트 실명 동의** — 없습니다 (`ANONYMIZE = true`로 마스킹 중)
3. **브랜드 컬러·로고 확정** — 현재 액센트는 임의값

### ⚠️ 캡처 이미지가 마스킹을 무력화합니다 (미해결)

`public/work/arise-layout.jpg` 에 **"부산대학교 AI 거점대학육성사업단"이 그대로 찍혀 있습니다.**
텍스트로는 "국립대학교 AI 교육·연구 조직"으로 가려두었는데 그림에서 발주처가 드러납니다.
`/` 와 `/project/platform/` 두 곳에 노출됩니다.

해당 사이트가 공개 운영 중이라 그 자체가 비밀은 아니지만 `ANONYMIZE`의 취지와 어긋납니다.
대표에게 세 가지 안(그대로 두기 / 기관명만 블러 / 부산대만 실명 허용)을 제시했고 **답을 못 받았습니다.**

### ⚠️ 정식 URL 방향이 배포처마다 반대입니다 (미해결)

| | 방향 |
|---|---|
| Vercel (현재 실서비스) | `kodekorea.kr` → 307 → `www.kodekorea.kr` (**www가 정식**) |
| 자체 서버 Caddy | `www.kodekorea.kr` → 301 → `kodekorea.kr` (**apex가 정식**) |

**DNS를 서버로 옮기면 정식 URL이 뒤집혀** 기존 링크와 검색 결과에 영향이 갑니다.
오랫동안 www가 정식이었으니 유지하려면 서버 Caddy를 www 정식으로 바꿔야 합니다.
이 질문도 **답을 못 받았습니다.** DNS 옮기기 전에 반드시 정리하세요.

### ⚠️ `nav` 배열에 항목을 앞에 끼우지 마세요

`app/c/hle`, `hut8`, `robin`, `sentra`의 여러 파일이 `nav[0]`~`nav[3]`을
**위치 인덱스로** 참조합니다. 앞에 하나 넣으면 그 페이지들 섹션 라벨이 조용히 한 칸씩 어긋납니다.
그래서 프로토타입 메뉴는 `nav`에 넣지 않고 `protoNav`로 따로 뺐습니다.

### 모션 검증은 브라우저 창이 **보이는 상태**에서만 됩니다

탭이 백그라운드면 브라우저가 `requestAnimationFrame`을 멈춥니다. 공유 티커(`ticker.ts`)가
백그라운드에서 스스로 멈추도록 **의도적으로** 만들었기 때문에, 이때 측정하면 회전 주기가 전부 0으로 나옵니다.
스크린샷은 정상적으로 찍혀서 "화면은 나오는데 모션만 안 돈다"로 보이고, 코드 버그로 오진하기 쉽습니다.

대안: 빌드 산출물의 주기값 grep (미니파이어가 `0.0001`을 `1e-4`로 바꾸니 원시 표기로 찾으면 놓칩니다),
프리렌더된 `out/index.html`의 `periodMs`(호출부 값은 JS 청크가 아니라 HTML에 들어갑니다), CSS duration.

### 모션을 한 번에 여러 개 바꾸지 마세요

한 라운드에 7개를 동시에 바꿨다가 **"원래보다 별로여졌는데 모든 수정사항이"** 라는 평가를 받고
전부 되돌렸습니다(`cd8f569`). 원인 두 가지였습니다:

1. 스크롤에 그림 그리기를 묶어서 읽는 동안 그림이 미완성으로 남았다
2. 스캔라인이 두꺼운 주황 띠로 보였다

**한 번에 하나씩 바꾸고, 프로덕션 빌드로 눈으로 확인한 뒤 넘어가세요.**

### 영상 태그는 쓰지 않았습니다

시도했지만 검증 환경에서 디코드가 렌더러를 얼려 확인이 불가능했습니다.
확인하지 못한 것은 싣지 않는다는 원칙으로, 정지 이미지 + 스크롤 켄번즈로 대체했습니다.

### 그 외 삽질 기록

| 증상 | 원인 / 해법 |
|---|---|
| 하이드레이션 완전 실패 (reveal 37개 전부 opacity 0) | 죽은 dev 서버가 포트를 물고 깨진 번들을 내주고 있었음. node 종료 → `.next` 삭제 → 새 포트 |
| `curl -H "Host: ..."`가 HTTPS에서 000 | SNI 불일치. 서비스 문제 아님. `--resolve` 사용 |
| Vercel 배포 3연속 실패 | ① `comment` 키 스키마 거부 → README로 분리 ② `framework: nextjs`가 `out/`에서 `routes-manifest.json`을 찾음 → `framework: null` ③ Next 15.5.4가 취약점으로 차단 → 15.5.22 업그레이드 |
| Vercel 배포 후 `Error: fetch failed` | 배포는 성공했고 **배포 후 상태 조회만** 실패한 것. 라이브를 직접 확인하면 됩니다 |
| 배포 명령을 백그라운드로 넘겼다가 중단 | 라이브가 갱신 안 된 채로 "배포됐다"고 착각할 수 있습니다. **배포 후 항상 라이브를 직접 긁어 확인하세요** |
| 레퍼런스 사이트 크롤링 불가 | 대부분 AI 크롤링 차단. 브라우저 띄워 조금씩 스크롤·캡처해서 **모든 이미지**를 분석하는 방식으로 진행 (`docs/references.md`) |

### 남의 메일함은 읽지 않았습니다

실적을 메일에서 뽑을 때 `busan01~22`, `sajik1~20`, `swdau.ai01~10` 등
**교육 참가자에게 발급한 계정의 본문은 열지 않았습니다.** 회사 자산이지만 제3자의 사적 통신입니다.
필요한 실적 정보는 계정 명명 규칙과 대표 계정 메일에서 확인했습니다. **이 선은 유지해 주세요.**

---

## 8. 다음에 해야 할 일

### 바로 (색인 열기 전 필수)

1. **개인정보처리방침 작성** — 없습니다. 푸터 링크가 `#`. 표준 템플릿으로 초안 잡고 대표 승인
2. **캡처 이미지 마스킹 결정** — §7. 대표 답변 대기 중
3. **정식 URL 방향 확정** — §7. DNS 옮기기 전 필수
4. **Netlify 처리** — 지금 Netlify에는 **프로토타입 섹션이 없는 구버전**이 떠 있습니다. 검토용으로 계속 쓸 거면 크레딧 충전, 안 쓸 거면 사이트를 삭제해서 혼동을 없애세요

### 그 다음

5. **브랜드 컬러·로고 확정** → 확정되면 아이콘 세트 제작 (지금 막혀 있는 이유가 이것)
6. **실기기 모바일 검증** — 미디어쿼리는 있지만 실제 폰에서 본 적 없음
7. **실적 대시보드 서버 저장 전환** — 현재 localStorage. `app/c/v2/posts.ts`의 `store` 객체 하나만 갈아끼우면 됩니다. 필요한 것: Supabase URL·anon key, `works` 테이블, RLS(select는 anon 허용, insert/delete는 인증 사용자만). 주석에 적어뒀습니다
8. **문의 폼** — 만들면 개인정보처리방침이 선행 필수
9. **버려진 시안 4벌 정리 판단** — `/c/hle` `hut8` `robin` `sentra`. 링크를 공유했어서 살려뒀는데, 이제 필요 없으면 지우고 `nav` 위치 참조 문제(§7)도 같이 해소

### 미완으로 남긴 조사

- `robin-thomas.me` Acte 4 게임플레이 미녹화 — 스크롤 **속도** 게이지 때문에 자동화로 진입이 안 됩니다. 대표가 직접 녹화해 준 영상으로 1~3막만 분석했습니다
- `hle.io` 모바일 렌더 미확인 — 미디어쿼리 값은 추출했으나 실제 렌더는 못 봤습니다

---

## 9. 자격증명 — 어디에 있나 (값은 여기 안 적습니다)

| 필요한 것 | 어디에 |
|---|---|
| 자체 서버 SSH 키 | 작업PC의 `~/.ssh/id_ed25519_new`. **인계 패키지에 없습니다** → 대표에게 요청하거나 서버에 새 공개키를 등록해야 합니다 |
| Vercel CLI 로그인 | 계정 로그인 방식. `.env.local`의 `VERCEL_OIDC_TOKEN`은 CLI가 자동 생성한 것으로 **재로그인하면 새로 생깁니다** |
| Netlify CLI 로그인 | 계정 로그인 방식 |
| GitHub 저장소 접근 | `https://github.com/mslopgk/kodekorea-webpage.git` — 푸시 권한 필요 |

> 이 리포에는 API 키가 없습니다. `.env.local`에 든 것은 Vercel CLI 토큰 하나뿐입니다.
> 인계 패키지의 `../../30_계정_API키/02_환경변수_값.md`는 **다른 프로젝트(부산 공공디자인 플랫폼)** 것이라 이 홈페이지와 무관합니다.

---

## 10. 상위 INDEX.md 정정

`코드코리아_기업_홈페이지/INDEX.md`에 사실과 다른 서술이 두 개 있습니다:

| INDEX.md 서술 | 실제 |
|---|---|
| "현재 로컬 Git에는 원격 저장소가 설정되어 있지 않습니다" | **틀림.** 원격이 있고 `master`가 푸시돼 있습니다: `https://github.com/mslopgk/kodekorea-webpage.git` (`origin/master` = `232d70c`, 로컬과 일치) |
| "자격증명은 → `../../30_계정_API키/SSH키/README.md`" | **그 경로가 없습니다.** `30_계정_API키/`에는 `02_환경변수_값.md` 하나만 있고, 그건 다른 프로젝트 것입니다 |

INDEX.md의 "인계 메모" 빈칸은 이 문서의 §5(담당자·약속), §1(저장소·도메인), §8(다음 할 일), §7(주의사항)로 채워집니다.

### 형제 폴더 (이 프로젝트와의 관계 = **추정**)

`원본/` 안에 `kodekorea-3d`와 `새 폴더 (4)`가 함께 있습니다.
**이 저장소는 두 폴더를 참조하지 않습니다.** 관계는 확인하지 못했습니다.

- `kodekorea-3d` — `model.glb`, `dist/`, `README.md`, `STATUS.md` 보유. **(추정)** 3D 에셋 실험용 별도 프로젝트
- `새 폴더 (4)` — `asset-manifest.json`, `static/`, `icons/`, `robots.txt` 보유. **(추정)** CRA류로 빌드된 산출물

### 참고 — 이 저장소가 아닌 것

작업 중 **부산대 홈페이지 4건**(장영실융합연구원 / AI대학 / AI융합교육원 / AX-PBL)을 동시에 진행했는데,
그건 **수주 프로젝트로 이 저장소와 별개**입니다. **(추정)** 별도 저장소이거나 다른 폴더에 있을 것입니다.
이 사이트에는 그 건이 *실적*으로만 등장합니다.

### 남아 있는 git 흔적

로컬 브랜치에 `worktree-wf_65f596bf-b23-1` ~ `-4` 4개가 옛 커밋(`f09405b`)에 멈춰 있습니다.
시안 4벌을 병렬 생성할 때 쓴 worktree 잔재입니다. 지워도 무해합니다.
